import { styled } from "@/styled/jsx"

export const Card = styled("div", {
  base: {
    borderRadius: "md",
    border: "secondary",
    backgroundColor: "surface.secondary",
    padding: "4",
  },
})
